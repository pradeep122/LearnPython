# My solution
# import utils as maximum
# maximum.find_max()

# Actual Solution:
from utils import find_max
numbers = [10, 3, 6, 2]
maximum = find_max(numbers)
print(maximum)
# print(maximum(numbers))  # TypeError: 'int' object is not callable
